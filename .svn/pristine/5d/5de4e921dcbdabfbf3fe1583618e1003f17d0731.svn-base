<div>
    <div ng-include src="'partials/error-partial.html'"></div>
    <h1>Les Livres :</h1>
    <a href="#/livres/add">+ Ajouter un livre</a> <br />
    <div class="container">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>#ISBN</th>
                    <th>Titre</th>
                    <th>Nb. Pages</th>
                    <th>Format</th>
                    <th>Langue</th>
                    <th>Editeur</th>
                    <th>Je l'ai&nbsp;?</th>
                    <th>Lu&nbsp;?</th>
                    <th>Créée le</th>
                    <th>Dernière M.A.J. le</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <tr ng-repeat="livre in livres">
                    <td>{{livre.isbn}}</td>
                    <td>{{livre.titre}}</td>
                    <td>{{livre.nbPage}}</td>
                    <td>{{livre.format}}</td>
                    <td>{{livre.langue}}</td>
                    <td>{{livre.editeur}}</td>
                    <td>
                        <span class="glyphicon glyphicon-ok" aria-hidden="true" ng-show="livre.possede"></span>
                        <span class="glyphicon glyphicon-remove" aria-hidden="true" ng-show="!livre.possede"></span>
                    </td>
                    <td>
                        <span class="glyphicon glyphicon-ok" aria-hidden="true" ng-show="livre.lu"></span>
                        <span class="glyphicon glyphicon-remove" aria-hidden="true" ng-show="!livre.lu"></span>
                    </td>
                    <td>{{livre.createdAt | date: 'dd/MM/yyyy à HH:mm:ss'}}</td>
                    <td>{{livre.updatedAt | date: 'dd/MM/yyyy à HH:mm:ss'}}</td>
                    <td>
                        <button type="button" class="btn btn-default btn-xs" ng-click="edit(livre.isbn)">
                            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                        </button>
                        <button type="button" class="btn btn-default btn-xs"
                            ng-click="del(livre.isbn)">
                            <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
                        </button>
                    </td>
                </tr>
            </tbody>
        </table>
        <div ng-include src="'partials/pagination-partial.html'" ng-show="livres.length > 0"></div>
        <p ng-show="livres.length == 0">Aucune donnée !</p>
    </div>
</div>